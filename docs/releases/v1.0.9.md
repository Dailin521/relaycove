# RichChat 1.0.9

- 修复图片预览打开后卡住或闪退的问题。预览改为独立裁切与变换容器，保留滚轮缩放、左键拖动和重置；透明 GIF 继续按原方式播放。
- 设置新增缓存位置。可选择本地磁盘目录，重启后把聊天缓存、收藏表情、表情缓存、更新包和通知头像迁移到 `RichChatData`，迁移校验完成后再清理原缓存。

版本 1.0.9，构建号 16。提供 Windows x64 中文安装器、自包含 ZIP、SHA-256 校验文件和自动更新清单。安装器未签名，安装后不会自动启动。

发布前已完成 Release 构建、四个普通测试项目、运行时与秘密检查、安装器及更新清单校验。未运行 Live、真实 Realm 写入、代码签名或干净 VM 升级验证。
