# 2026 年 9 月工作日志

本月于 2026-09-17 开始按月记录。下面的“历史整理”按现有源码、版本说明和已记录验证结果回补，日期是整理日期，不代表每项功能的原始开发日期。没有证据的逐日过程和人工验收不补写。

## 2026-09-17 — 1.0.7 已发布功能（历史整理）

- 来源：[1.0.7 说明](../releases/v1.0.7.md)、[STATUS](../ai/STATUS.md)、提交 `f3a30e9`，前一版 `04d7c20`。更早更新分别见 [1.0.5](../releases/v1.0.5.md)、[1.0.6](../releases/v1.0.6.md)，不计作本版新增。

| 问题/需求 | 最终变化 | 主要范围 |
|---|---|---|
| 表情浏览到底不加载、跳动或崩溃 | 超过 90% 首次触发分页，追加保持位置，补齐原生滚动监听；移除分类及底部说明 | StickerPagingPolicy、StickerInfiniteScrollBehavior、StickerPickerViewModel |
| 透明图片显示有底色或边框 | 纯图片消息透明气泡、GIF 逐帧透明渲染及加载取消处理 | RealmMediaImageView、TransparentGifFrameRenderer、RealmImageSourceService |
| 查看个人所在群聊 | 左侧“+”增加已加入且符合产品条件的群聊列表 | MainPage、ShellViewModel |
| 私聊搜索和日期 | 本地匹配与延迟服务端搜索、结果日期时间；发布复核补 UI 线程派发 | ShellViewModel、SearchResultItem |
| 文字首行靠下 | 减少正文上方空白 | MessageListView |
| 退出后进程残留、再次启动无窗口 | 幂等退出、异步清理和 3 秒兜底；单实例激活等待与窗口恢复 | ApplicationShutdownCoordinator、Windows App、ClientSession.StopAsync |
| 文件已下载仍显示下载 | 当前账号附件标识匹配，检查文件存在后显示打开，重启恢复 | DownloadHistoryEntry、MessageAttachmentItem、ShellViewModel |
| 重复点击下载与进度反馈 | 下载中禁用，边框从顶部顺时针推进，未知长度循环，失败/取消复位 | DownloadBorderProgress、MessageAttachmentItem、ShellViewModel |
| 图片不能直接复制 | 右键复制图片写入 Windows 位图剪贴板；GIF 首帧，失败处理与账号切换保护 | MauiPlatformInteractionService、ShellViewModel、MainPage |

- 验证：发布前实际运行 `pwsh ./scripts/verify.ps1 -Mode Full`，Core 307、Client 324、Data 41、App 1092，共 1764 项通过；0 编译错误、7 个既有 XamlC 提示。独立只读复核发现搜索跨线程集合更新，修复并加入回归后重新 Full 通过，二次复核通过。原始本机日志 `.verify/release-1.0.7-full.log` 不进入 Git。
- 人工反馈：用户明确确认表情滚动正确，并在图片复制完成后要求发布；不将此反馈扩展为所有原生场景已经专项验收。
- 限制：未运行 Live、真实 Realm 写入、故障 Win10 设备复现、干净 VM 安装升级或签名；跨应用图片粘贴兼容性未做专项验证。旧附件记录缺少标识需重新下载后识别，GIF 剪贴板为静态首帧。
- 交付：已推送 `main` 提交 `f3a30e9`，已发布 [v1.0.7](https://github.com/Dailin521/relaycove/releases/tag/v1.0.7)，构建 14；发布时已核对远端 HEAD/tag、Latest 状态、五项资产长度及 SHA-256。

## 2026-09-17 — 协作规则与功能文档补全

- 需求：每次功能修改都补工作日志，根据现有资料完善功能文档。
- 变化：AGENTS 明确所有功能/缺陷/视觉调整须在交付前记日志，并更新相关功能说明；新增按月日志与模板、功能说明、AI 索引和工作流、UI 文档入口，回补 1.0.7 版本说明；移除失效 V2/交互规范引用及“仅维护临时 Stage 记录”的冲突约定。自动检查更新入口按实际代码更正为账户页。
- 范围：仅 AGENTS、根目录文档和 docs；没有修改产品代码、版本或发布资产。
- 验证：对照当前 XAML、ViewModel、服务、既有版本记录；设置/群聊/联系人由独立只读梳理辅助核对。50 个本地文档链接检查全部通过，`git diff --check` 通过。纯文档改动不重新运行 Full，不操作用户窗口。
- 限制：功能说明中的“已实现”不替代原生设备验收；历史整理只引用已有证据。本次没有新增设备或服务器验证结论。
- 交付：本地文档已更新，未提交或推送。

## 2026-09-17 — 私聊搜索仅采用服务器结果

- 需求：用户反馈点击搜索仍像本地查询，确认改为服务端结果为准。
- 变化：移除 `ProjectSearch` 对内存消息的追加；每次重新提交先清空旧结果和分页游标。输入延迟搜索与显式提交使用同一路径，无命中为空，离线/错误不回退本地；错误与无结果提示区分。左侧导航搜索不变。
- 范围：[ShellViewModel](../../src/RelayCove.App/ViewModels/ShellViewModel.cs)、[回归测试](../../tests/RelayCove.App.Tests/ServerOnlySearchTests.cs)，同步功能说明第 7 节与 STATUS。
- 验证：`dotnet test tests/RelayCove.App.Tests/RelayCove.App.Tests.csproj -c Debug --no-restore --nologo --verbosity minimal -p:BaseOutputPath=E:/WorkSpace/RelayCove/.verify/server-only-search/ -p:UseAppHost=false --filter "FullyQualifiedName~Search|FullyQualifiedName~ConversationFilter"`，App 编译及 70 项测试通过。覆盖有本地匹配但服务器为空/失败、空关键词、仅展示服务器页、重新提交期间清空旧结果和失败后不恢复旧内容；`git diff --check` 通过。
- 限制：未访问真实 Realm，未操作用户窗口；分页加载的既有线程风险不在此次修改范围。请在 VS 使用有本地消息的关键词验证服务端无命中/断网时结果与提示。
- 交付：本地修改完成，待用户验证；未提交或推送，不改变已发布 1.0.7。

## 2026-09-17 — 安装器自动关闭只隐藏到托盘

- 需求：用户反馈各版本安装包选择自动关闭会卡住，必须手动从托盘退出才继续。
- 变化：新增 `scripts/installer/CloseInstalledApp.iss`，安装前按完整路径核验运行中的目标，明确同意后使用同一进程句柄结束并等待；默认否，拒绝/失败阻止继续，其他路径不终止。应用新增 `WindowsRestartManagerShutdown` 窗口消息处理，只有正式退出确认接入统一退出，查询/取消与用户普通关闭不触发真退出。
- 范围：安装器脚本、WindowsWindowShellAdapter、退出入口枚举、新增消息回归测试及隔离安装测试。未修改运行中进程，保留既有搜索与文档改动。
- 验证：App 定向编译及 `WindowsRestartManager|WindowsWindowShellAdapter|ApplicationShutdown` 22 项测试通过；`pwsh ./scripts/test-installer-close.ps1 -IsccPath .verify/installer/tools/inno-6.5.4/ISCC.exe` 和追加 `-RejectShutdownQuery` 均通过，覆盖原正常关闭失败、拒绝关闭不安装、确认后替换目标文件且不同目录同名进程存活；`git diff --check` 通过。
- 复核：独立只读复核发现进程访问/路径查询失败会静默跳过，已改为失败关闭并再次复核通过。无法识别的其他同名进程可能阻止安装，但不会被盲目终止。
- 限制：测试使用隔离隐藏进程，不是用户实际 MAUI 安装升级验收。尚未发布新版安装器；现有旧安装器不会自行获得该修复。强制结束可能丢失未保存草稿，安装提示要求先保存。
- 交付：本地修复完成，未提交、未推送、未发布，待实际新版安装升级验证。

## 2026-09-17 — 表情面板页签顺序与默认页

- 需求：第一项为原默认小表情，名称“表情”；第二项为原搜索表情，名称“gif”。
- 变化：MainPage 页签依次“表情 / gif / 收藏”，调整列位置及选中态对应关系；StickerPickerViewModel 初始及每次重新打开面板都选择 default，不因默认打开触发外部目录读取。gif 仍保留原搜索/分页/发送行为，不增加文件类型过滤。
- 验证：独立输出 `.verify/sticker-tab-order/` 下 App 编译及 `Sticker|MainShellLayoutTests` 97 项测试通过；新增默认打开/重新打开及不读取目录的回归，原搜索分页测试显式切换 gif；XAML 顺序、名称、列号和命令参数检查通过。日志 `.verify/sticker-tab-order.log`。
- 限制：未操作用户窗口，实际页签布局留待 VS 验证。
- 交付：本地完成，功能说明和计划已同步，未提交或推送。

## 2026-09-17 — 1.0.8 发布准备

- 用户已确认本月上述本地改动并授权发布；包含服务器搜索、安装器退出、表情页签及文档规则，版本升级为 1.0.8 / 构建 15。前述“未发布/待确认”为当时交付状态，本条更新其状态。
- 实际运行 Full：Core 307、Client 324、Data 41、App 1103，共 1775 项通过；Release 构建零错误、7 个既有警告，自包含 ZIP 与运行时/秘密检查通过。日志 `.verify/release-1.0.8-full.log`。
- 独立只读发布复核未发现新增 P0/P1；保留既有搜索分页跨线程 P2，不扩大本轮范围。未操作用户窗口、未运行 Live 或真实 Realm 写入、未作干净 VM 安装及代码签名。
- 打包：中文安装器编译通过，更新清单版本/构建号及安装器大小、SHA-256 一致；日志 `.verify/release-1.0.8-installer.log`。49 个本地文档链接检查通过。
- 交付目标：[v1.0.8](https://github.com/Dailin521/relaycove/releases/tag/v1.0.8)，五项资产（安装器、ZIP、各自校验文件及自动更新清单）；发布后以 GitHub tag 与资产为准。

## 2026-09-17 — 自定义缓存目录与重启迁移

- 需求：避免缓存持续占用 C 盘；用户选择迁移已有聊天缓存与收藏，重启后生效。
- 变化：设置 → 存储增加当前缓存路径、选择父目录和取消待迁移操作，目标为父目录下 RichChatData。启动取得单实例后、构造业务服务前显示迁移窗口，复制所有账号目录（含 SQLite 附属日志）、收藏、表情缓存、更新包及通知头像；逐文件 SHA-256 校验、提交配置后再校验并删除原文件。凭据、偏好和固定路径配置不迁移，附件下载位置独立。
- 恢复：复制中断的专属暂存目录按持久 ID/owner 重建；目标改名后配置提交中断可校验清单和源文件集合后恢复。源变化或损坏清单保留原路径，允许取消/重选；已激活磁盘不可用时明确停止启动，不回退空库。禁止网络盘、重叠路径、联接/符号链接及覆盖已有目标。退出进度窗口结束本次启动，原数据在提交前保留。
- 范围：StorageLocationService、StorageSettingsViewModel、WindowsStorageFolderPicker、Windows 启动入口、MauiProgram 依赖配置、ShellViewModel 和 SettingsPageView；同步功能说明、架构计划和 STATUS。
- 验证：隔离输出 `.verify/storage-location/` 运行 App 全部 1119 项测试通过；Data 在独立输出下 41 项通过。最后的提前取消及选择器异常处理调整后重新编译，16 项存储回归全部通过。命令为 `dotnet test` 对应项目，`-c Debug --no-restore --nologo --verbosity minimal -p:BaseOutputPath=<隔离目录>`，App 额外 `-p:UseAppHost=false`；最后定向使用 `--filter FullyQualifiedName~StorageLocationServiceTests`。日志 `.verify/storage-location-app-tests.log`、`.verify/storage-location-data-tests.log`、`.verify/storage-location-tests.log`；`git diff --check` 通过。
- 独立只读复核：修复损坏迁移记录阻止启动、活动 Data/Cache 子目录链接漏检、映射网络盘漏检；暂存目录恢复和删除边界再次复核无阻断。测试覆盖默认路径、延迟生效、取消、WAL/收藏保留、路径冲突、磁盘不可用、配置提交中断、源变化、损坏清单及重复迁移。
- 限制：未迁移用户真实数据，未运行用户窗口、实际跨盘/原生选择器、断电或真实数据库故障专项验证；未运行 Fast/Full/Live 或打包。已有选择器交互、长迁移窗口、重启后的真实聊天/收藏由用户在 VS 验证。目标盘需要容纳数据副本；清理失败只提示旧文件残留，不指导删除包含固定配置的整个原目录。
- 交付：本地修改完成，未提交、未推送、未发布。最短验证：存储中选择 D/E 盘父文件夹，确认待迁移路径，从托盘退出并重开，检查路径、聊天历史和收藏；再重启一次确认路径保持。

## 2026-09-17 — 图片预览滚轮缩放和左键拖动

- 需求：图片预览支持滚轮向前放大、向后缩小，按住左键移动。
- 初始变化：仅 IsPreview 图片启用 WinUI 鼠标输入，围绕鼠标位置缩放，范围为适应窗口大小的 0.25–8 倍；指针捕获确保拖出区域仍能释放，取消/失去捕获/卸载清理拖动状态。变换只作用于图片，内容裁切在预览区域，加载提示与现有 GIF 解码/播放不改变。增加重置按钮；初始实现曾在尺寸变化时重置，后续原生修复已按最新计划改为保留变换，最终行为以本月“专用容器修复”条目为准。
- 范围：RealmMediaImageView 预览分部类、ImagePreviewTransform、MainPage XAML/按钮事件及对应测试；保留上一轮缓存目录的全部本地修改。
- 验证：`dotnet test tests/RelayCove.App.Tests/RelayCove.App.Tests.csproj -c Debug --no-restore --nologo --verbosity minimal -p:BaseOutputPath=E:/WorkSpace/RelayCove/.verify/image-preview/ -p:UseAppHost=false --filter "FullyQualifiedName~ImagePreviewTransformTests|FullyQualifiedName~RealmMedia|FullyQualifiedName~MainShellLayoutTests"` 编译及 79 项通过。新增四项覆盖缩放方向/鼠标锚点、上下限、拖动与重置、未布局时不缩放；日志 `.verify/image-preview-tests.log`，`git diff --check` 通过。
- 限制：未启动或操作用户窗口，未把数学/媒体测试视为鼠标、DPI、GIF 原生效果验收；未运行 Full/Live 或打包。
- 交付：本地完成，未提交或推送。VS 最短验证：打开普通图片及 GIF，鼠标位于不同位置前后滚动、按住左键拖动并移出预览区域后松开，点击重置；关闭再开另一张图，检查恢复居中且工具栏不被图片遮挡。

## 2026-09-17 — 图片预览打开闪退排查与事件修正

- 反馈：加入缩放拖动后，点击图片预览闪退。
- 证据：只读查看 Windows Application 事件，16:44/16:45 两次 Debug RichChat 崩溃，模块 Microsoft.UI.Xaml.dll，异常 0xc000027b，WER 标识 0x80004002。使用本机 dotnet-dump 读取已有转储，当前线程未保留托管异常，仅见 WinUI Application.Start；没有取得能确定具体抛错语句的堆栈，接口转换根因仍属推断。未读取/输出聊天内容，转储和诊断工具不入库。
- 变化：RealmMediaImageView.Preview 将通用 AddHandler/RemoveHandler 的 object 委托注册改为强类型 PointerWheelChanged/PointerPressed 事件，对称解绑；重复挂接相同原生根节点时直接返回。缩放、拖动、GIF、裁切和重置行为保留。
- 验证：隔离输出下 App 编译、ImagePreviewTransformTests/RealmMedia/MainShellLayoutTests 共 80 项通过；新增预览输入强类型注册和对称清理的静态回归约束。命令与上一条相同，日志 `.verify/image-preview-crash-fix-tests.log`；`git diff --check` 通过。
- 限制与交付：未运行或操作用户 MAUI 窗口，尚未现场复现或证实原生闪退消失；请 VS 完整停止调试、重新生成并启动，再点开图片及 GIF，连续关闭重开后验证滚轮/拖动。本地修正未提交/发布，保留缓存目录功能改动。

## 2026-09-17 — 图片预览原生崩溃隔离与专用容器修复

- 反馈：强类型事件修改后仍在点开图片、未操作鼠标时卡住闪退。本次按用户授权建立独立 MAUI/WinUI 测试窗口，使用生成的本地图片和替代服务，不登录、不访问网络或真实缓存、不操作现有聊天窗口。
- 定位：保存旧 App 程序集，以实际 RealmMediaImageView 逐项隔离。旧预览无需输入即在首次显示崩溃；去掉鼠标事件、仅设置原生 Clip 仍崩溃。仅显示、仅事件、仅变换以及移除 Clip 的预览通过。Windows 事件为 Microsoft.UI.Xaml.dll、0xc000027b，WER 80004002，与用户故障一致；这是受控复现证据，未取得具体原生抛错调用栈。前一条事件注册调整未解决问题。
- 修复：新增 ImagePreviewViewport，外层 Grid 裁切、内层 Grid 缩放平移，复用原图片解码及透明 GIF 播放，不覆盖通用媒体控件原生 Clip。加载后绑定输入，卸载/更换原生节点解绑并停止拖动；关闭、换图、重置恢复初始状态，尺寸变化保留用户变换。阶段日志仅含阶段、异常类型/错误码，不写图片内容、消息或凭据。
- 验证：隔离输出 `.verify/preview-isolated-fix/` 编译及 80 项定向测试通过；随后同一编译产物运行全部 App 测试，1124 项通过。命令为 `dotnet test tests/RelayCove.App.Tests/RelayCove.App.Tests.csproj -c Debug --no-restore --nologo --verbosity minimal -p:BaseOutputPath=E:/WorkSpace/RelayCove/.verify/preview-isolated-fix/ -p:UseAppHost=false`，定向追加 `--filter "FullyQualifiedName~ImagePreviewTransformTests|FullyQualifiedName~RealmMedia|FullyQualifiedName~MainShellLayoutTests"`，全量追加 `--no-build`。日志 `.verify/preview-isolated-fix-build.log`、`.verify/preview-isolated-fix-app-tests.log`。
- 原生验证：独立测试宿主引用隔离 App 程序集；修复版真实 RealmImageSource/服务链的 PNG、JPEG、透明 GIF、4096×3072 图片解码检查通过；静置、20 次加载卸载、程序调用缩放/平移/重置、尺寸变化保留缩放、加载中关闭取消均通过，并断言卸载后原生输入引用清空。证据 `.verify/preview-native-fixed-parity/phases.log`；旧版对照 `.verify/preview-native-old/`、`.verify/preview-native-clip-only/`、`.verify/preview-native-events-only/` 等目录。没有把程序调用变换当作真实鼠标输入验收。
- 可复用验证：新增 `tests/RelayCove.Preview.NativeTests` 与 `scripts/test-image-preview.ps1`，独立于普通 Fast/Full。脚本需当前用户同意可见测试并显式传 `-RunVisibleNativeTests`，超时只结束自身保存句柄对应的测试进程；脚本本轮只作语法检查，宿主已单独编译运行。测试说明见该项目 README。
- 独立只读复核：未发现代码 P0/P1；修正早期日志与最终尺寸变化行为的表述差异。主代理已回读原生测试断言和结果，`git diff --check` 通过。
- 限制与交付：用户反馈可见循环闪眼后立即停止后续可见测试，已确认无残留测试进程。未执行真实鼠标滚轮/拖动/窗口外松开验证，用户可在 VS 正常打开一张图片检查交互；不再要求反复开关。功能说明与 STATUS 已同步，缓存目录等既有修改保留；未提交、推送或发布。

## 2026-09-17 — 1.0.9 发布准备

- 需求：用户确认缓存位置与图片预览修复无问题，要求提交、推送并发布新版本。
- 变化：版本升为 `1.0.9 / 16`；本版包含缓存目录选择与重启迁移、图片预览原生闪退修复及其滚轮缩放、拖动、重置交互。同步更新版本说明、功能说明、产品计划、AI 索引和当前状态。
- 验证：`pwsh ./scripts/verify.ps1 -Mode Full` 通过，Release 构建 0 错误、7 个既有 XamlC 警告；Core 307、Zulip.Client 324、Data 41、App 1124，共 1796 项通过。自包含 ZIP 及运行时/秘密检查通过，日志 `.verify/release-1.0.9-full.log`。`pwsh ./scripts/package-installer.ps1 -IsccPath .verify/installer/tools/inno-6.5.4/ISCC.exe` 通过，生成安装器、两个 SHA-256 文件和更新清单，日志 `.verify/release-1.0.9-installer.log`。
- 限制：未运行 Live、真实 Realm 写入、代码签名、干净 VM 升级或原生鼠标专项自动化。此前用户已在 VS 确认图片预览可用；缓存跨盘迁移仍以实际使用环境为准。
- 交付：已提交并推送 `6f322e2`，已创建并推送 `v1.0.9`，已发布 [RichChat 1.0.9](https://github.com/Dailin521/relaycove/releases/tag/v1.0.9)。GitHub 回读确认 Latest、标题、版本说明、目标 `main`、五项资产、安装器/ZIP 长度和 SHA-256，以及更新清单 `1.0.9 / 16` 一致。

## 2026-09-17 — Android / iOS 移动适配文档

- 需求：用户确认先在 GitHub 仓库落实移动适配文档，不实施功能改造。
- 变化：新增 [移动适配计划](../MOBILE_ADAPTATION.md)，记录基线源码障碍、共享与平台实现边界、手机内测范围、M1–M4 任务、生命周期/文件/安全存储、签名分发及最小验证要求；后台推送独立待规划。同步 AI 活动计划索引、产品范围和 STATUS，保留 Windows 功能和当前版本。
- 范围：仅 `docs/MOBILE_ADAPTATION.md`、`docs/ai/README.md`、`docs/ai/STATUS.md`、根目录产品计划及本日志；不修改源码、功能说明中的已实现行为、版本、CI、发布脚本或服务器。
- 验证：通过 GitHub 读取 `5477b227ab2c63bb9cd16c1762b27de8073fd2f6` 基线和相关源码/文档，核对 Microsoft MAUI 官方平台、生命周期、存储和发布说明；源码障碍与待设备验证项分开记录。未运行 Fast/Full/Live、移动构建、应用窗口、真机、签名或打包。
- 限制：Mac/Xcode、Android 工具链、最低系统版本、测试设备及签名/分发材料仍待实施阶段确认；已有 Windows 测试不作为手机验收证据。本次不声称已能生成 Android/iOS 安装包。
- 交付：按用户授权纳入本次文档提交；实现阶段未开始，远端提交号以 Git 历史为准；不创建 tag 或 Release。
