# RichChat 当前状态

更新：2026-09-14

- 当前源码：`1.0.5`，构建号 `12`，位于 `main`；以 SVN r4992 同步基线为基础。
- GitHub Release 更新检查已实现；默认启动检查，每个新版提示一次，支持手动下载、取消和打开安装包。按构建号识别更新，不误用旧 `2.4.0` 包。
- 用户已展示本机“关于 → 软件更新”结果，并要求发布新版验证完整更新路径。表情包搜索及个人收藏尚未实现。
- 本轮 Full 通过：Core 293、Zulip.Client 311、Data 41、App 980，共 1625 项；Release 构建 0 error、7 个现有 XamlC 性能提示，自包含发布及秘密检查通过。
- 独立只读复核无未解决 P0/P1；“旧 ZIP 被标成新构建”问题已通过核对包内程序版本修复，并有回归测试。
- 未运行 Live、真实 Realm 写入、干净 VM、代码签名或实际安装升级验收；这些结果不能由单元测试或构建代替。
- 本次不回写 SVN；本机 `.verify/` 和发布产物不进入提交。
