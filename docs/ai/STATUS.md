# RichChat 当前状态

更新：2026-09-17

- 发布候选：`1.0.8`，构建号 `15`；用户已确认本轮修改并授权发布。保持 SVN 的 `1.0.x` 版本系列，不回写 SVN。版本变化见 [1.0.8 说明](../releases/v1.0.8.md)。
- 搜索仅采用服务器结果，新请求清空旧结果，失败与无命中分别提示；中文全文搜索依赖服务器配置，本轮未修改服务器。已知分页“加载更多”的后台线程集合更新 P2 尚待单独处理，本轮未扩大范围。
- 安装器确认后按目标完整路径及保留句柄结束旧实例，拒绝或核实失败则阻止继续；新版应用接入 Restart Manager 正式退出确认。22 项定向测试及两种隔离安装测试通过，其他目录同名进程未受影响；独立只读复核通过。实际设备升级仍待验证。
- 表情页签为“表情 / gif / 收藏”，每次打开先显示原默认小表情，gif 保留搜索及分页。97 项定向测试及 XAML 映射检查通过，用户已确认。
- 已补齐功能说明、AI 索引、工作流、按月工作日志；AGENTS 要求每次功能修改在交付前记录并同步文档。
- 附件按当前账号下载历史和本地文件存在性显示“下载/打开”；下载中禁用重复点击，边框从顶部中央顺时针显示进度，未知长度时循环。旧历史缺少附件标识，重新下载后可识别。
- 图片右键支持“复制图片”，原图解码后保留 alpha 写入 Windows 剪贴板；GIF 复制首帧。纯图片消息无气泡背景和边框，透明 GIF 使用逐帧透明渲染。
- 表情列表首次越过 90% 触发分页，使用 `KeepScrollOffset` 保持追加前位置，补齐模板延迟创建和生命周期监听；用户已确认滚动行为恢复正常。支持空关键词浏览，不显示分类和底部说明。
- 左侧“+”可查看自己所在群聊；私聊搜索展示服务端结果和日期；消息正文减少顶部留白。
- Windows 主窗口实际销毁与托盘退出统一清理，独立 3 秒退出兜底；关闭到托盘行为保留。单实例激活和窗口恢复增加等待、竞争处理及本地生命周期诊断。故障 Win10 设备尚未复现验证，不能把源码风险称为已确认根因。
- 独立只读复核发现延迟搜索在线程池修改绑定集合，已改为 UI dispatcher，并在派发回调内验证查询和账号；回归测试覆盖集合变化必须经 dispatcher，二次复核通过。无未解决的已知 P0/P1。
- 1.0.8 发布前 `pwsh ./scripts/verify.ps1 -Mode Full` 通过：Core 307、Client 324、Data 41、App 1103，共 1775 项；Release 构建 0 错误、7 个既有警告，自包含 ZIP 及运行时/秘密检查通过。证据：本机 `.verify/release-1.0.8-full.log`。独立发布复核未发现新增 P0/P1。
- 发布产物：Windows x64 自包含 ZIP、中文 EXE 安装器、两个 SHA-256 文件与 `update-win-x64.json`，必须同批打包且版本为 `1.0.8 / 15`。更新服务以构建号判定新旧，不使用历史 `2.4.0` 版本号排序。
- Agent 未操作用户 MAUI 窗口；用户确认不代替 Win10 故障设备、干净 VM 安装升级、真实 Realm 或原生剪贴板兼容性专项验证。本轮未运行 Live、真实 Realm 写入、代码签名或干净 VM；安装器未签名。
- `.verify/`、本机诊断和发布产物不进入 Git 提交。
