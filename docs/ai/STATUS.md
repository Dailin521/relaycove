# RichChat 当前状态

更新：2026-09-16

- 发布候选：`1.0.6`，构建号 `13`；以 `1.0.5` 为基线，包含 SVN r5027 同步、Windows 空闲状态上报、群聊创建错误提示、消息复制和表情面板。用户已通过 Visual Studio 确认本轮消息复制及表情面板布局。
- 表情面板包含搜索/默认/收藏三页：搜索结果只显示图片；默认小表情使用更大单元格并收紧高度，收藏保留名称及管理操作。真实 Realm、干净 VM、代码签名和实际安装升级验收仍未运行。
- GitHub Release 更新检查已实现；默认启动检查，每个新版提示一次，支持手动下载、取消和打开安装包。按构建号识别更新，不误用旧 `2.4.0` 包。
- 表情面板已增加搜索/默认/收藏三页：ChineseBQB 名称及分类搜索、60 张分页、本机图片导入、搜索结果/聊天图片收藏、收藏改名和移除。默认组织小表情继续插入输入框；大图独立发送，保留文字和附件草稿。收藏按账号隔离，GIF 保留原始字节。
- 发布验证：`pwsh ./scripts/verify.ps1 -Mode Full` 已通过；Release 构建 0 error、7 个既有 XamlC 性能提示，自包含 ZIP、中文 EXE 安装器、两个 SHA-256 文件和更新清单均已生成并核对为 `1.0.6` / 构建号 `13`。
- 用户反馈“已加入表情收藏”卡片常驻后，已移除收藏操作向聊天区附件下载状态栏写入提示的逻辑；收藏仍正常执行，也不会覆盖正在下载的状态。当前修正编译及 3 项相关回归通过（独立输出目录，避开运行中 VS/RichChat 的文件占用）：`.verify/sticker-collect-banner-isolated.log`。待用户停止调试并重新运行确认。
- 公开源实际读取通过：5871 张、114 类、1280 个动图；下载一张猫咪的 WebP 缩略图与 225140 字节 GIF，缓存回读原始字节一致。首次请求不可用后重试成功；首次搜索仍需要可用网络。证据：`.verify/sticker-source-probe.log`。
- 独立只读复核发现的“发送结果未知误报取消”和“损坏缓存无法重试”已修复并增加回归测试；无未解决 P0/P1。账号/会话切换、连续点击、上传失败不自动重试、草稿保留、关闭后丢弃晚到结果均有确定性测试。
- 待人工验证：打开表情面板切换三页、搜索及滚动分页、导入/收藏/改名/移除、GIF 悬停及消息播放、缩小窗口与关闭面板；发送时检查草稿仍在。未操作用户 MAUI 窗口，单元测试不代替原生交互验收。
- 未运行 Live、真实 Realm 写入、干净 VM、代码签名或实际安装升级验收；这些结果不能由单元测试或构建代替。
- 本次不回写 SVN；本机 `.verify/` 和发布产物不进入提交。
