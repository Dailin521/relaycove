# AI 任务模板

复制本模板到 `docs/ai/tasks/YYYY-MM-DD-stage-N-short-name.md`。任务开始时填写前半部分；完成验证后填写结果部分。不要记录完整聊天或思考过程。

## 任务定义

- **任务名称：** `<阶段与简短名称>`
- **状态：** `待开始 | 进行中 | 已完成 | 已阻塞`
- **基准提交：** `<git rev-parse HEAD>`
- **工作分支：** `agent/stage-<编号>-<slug>`
- **相关方案章节：** `<章节编号或链接>`

### 目标

用一至三句话描述本任务完成后的可观察结果。

### 已知事实

- `已验证`：列出从仓库、命令或官方文档确认的事实。

### 假设

- `假设`：列出尚未验证但暂时采用的判断。

### 范围

- 必须实现：
  - `<行为或交付物>`
- 允许修改：
  - `<目录、模块或文件>`
- 明确不做：
  - `<相邻功能、重构或未来阶段内容>`

### 验收标准

- [ ] `<可以明确验证的结果>`
- [ ] `<失败或边界场景>`

### 验证命令

```powershell
<精确命令；尚无脚手架时填写文档检查命令>
```

### 停止并询问

- `<本任务特有的歧义、兼容性或安全边界>`
- 同时遵守 `AGENTS.md` 与 `docs/ai/WORKFLOW.md` 的通用停止条件。

## 执行提示词

```text
阅读 AGENTS.md、相关工程方案章节、docs/ai/STATUS.md 和本任务文件。
只实现“范围”中的内容，不处理“明确不做”的事项。
先确认仓库事实和基线，再完成最小可验收实现。
运行列出的验证；未运行的项目必须标注“未验证”并说明原因。
触发停止条件时保留现场并询问，不自行扩大权限或范围。
完成后更新本文件的结果部分；验证通过后允许本地提交，但不得推送或合并。
```

## 任务结果

### 修改摘要

- `<新增或修改的行为>`

### 验证证据

| 状态 | 命令或场景 | 结果 |
| --- | --- | --- |
| `已验证` | `<命令>` | `<退出码和关键结果>` |
| `未验证` | `<检查>` | `<未执行原因>` |

### 文件范围

- 新增：`<路径>`
- 修改：`<路径>`
- 删除：`<路径或“无”>`

### 决策与限制

- 决策：`<仅记录会影响后续任务的选择；没有则写“无”>`
- 已知限制：`<限制或“无”>`

### 下一步

- `<单一、明确的后续任务>`
