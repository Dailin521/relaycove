# RelayCove 2.1.0 — V2.1 release

Status: in progress; verification and publication pending

## Purpose

- Synchronize the current `main` delivery and publish the Stage 31–36 increment as the formal Windows 11 x64 `v2.1.0` GitHub Release.
- Preserve the existing `v2.0.0-alpha.1` tag and release unchanged.
- Treat V2.1 as a release checkpoint; do not mark the complete ordered V2 optimization plan finished.

## Release scope

- Selectable ordinary message text with native Windows drag selection and copy.
- Saved-message entry, pagination and precise message anchoring.
- Reaction button clipping correction and trigger-relative reaction-picker positioning.
- Image-only message presentation and contextual original-image download.
- Persistent Windows tray icon, unread-only hover preview, in-place attention blink and click-through conversation navigation.

## Version and distribution

- Application display/informational version: `2.1.0`; Windows application version: `2`.
- Tag: `v2.1.0`.
- Asset: `RelayCove-2.1.0-win-x64.zip`.
- Integrity manifest: `RelayCove-2.1.0-win-x64.sha256`.
- The ZIP remains unpackaged, self-contained and unsigned, with no automatic updater.

## Known boundaries

- Windows 11 x64 only; no installer, MSIX, code signing or background push.
- Stage 32 saved-message exact-position and Stage 34 reaction-picker positioning still lack their final separate user confirmation, despite delivered code and focused tests.
- Stage 21 clean Windows 11 VM startup and final formal password-login gates remain open.
- No Live suite, real Realm write, App startup or user-window operation is part of this release operation.

## Verification evidence

- Synchronized baseline: `main@f221424f9a7e88fb874f86c5e1482bb6e96552e0`, equal to `origin/main` before release preparation.
- The first invocation used the previous broad Full definition and stopped at its repository-wide `dotnet format --verify-no-changes` line-ending debt. The user then simplified the release flow for the MAUI-only product: Fast is Debug build plus four ordinary tests; Full independently covers Release build/tests, app-only publish, ZIP/runtime/content and secret checks. Default verification no longer runs the unusable format gate or historical Web checks.
- The simplified `pwsh ./scripts/verify.ps1 -Mode Full` passed. Release solution build completed with 0 errors and 7 existing XamlC binding-performance warnings. Release tests passed Core 151/151, Zulip.Client 102/102, Data 34/34 and App 300/300. App-only self-contained publish, required runtime/license checks, secret scan, ZIP and manifest generation passed.
- Debug and historical Web checks had already passed before the workflow was simplified, but they are supplementary evidence and are not part of the V2.1 release gate.
- `dotnet list RelayCove.sln package --vulnerable --include-transitive` completed against NuGet advisory data and reported no vulnerable package in any of the nine solution projects.
- The simplified Full pre-commit package produced 661 files and included the required application, .NET runtime, SQLite, license and notice entries. Runtime-data, forbidden-file and publish-text credential scans passed. Its ProductVersion is `2.1.0+f221424f9a7e88fb874f86c5e1482bb6e96552e0`; it is diagnostic only and must not be published because the release preparation is not committed. No dirty-tree ZIP hash is treated as release evidence.
- Two independent read-only reviews found no script/security boundary regression. They correctly blocked both dirty-tree trial packages and required a clean rebuild from the exact committed candidate. The version script now asserts display, informational and Windows application versions before Full packages.
- Final clean committed artifact evidence: pending.

## Publication evidence

- Release candidate commit, annotated tag target, GitHub Release URL, uploaded asset digest and final remote verification: pending.
