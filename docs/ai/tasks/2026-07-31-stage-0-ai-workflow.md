# 阶段 0：建立 AI 开发工作流

## 任务定义

- **状态：** 已完成
- **基准提交：** `b8233086bebcc68b2e2e944fc58e78f1e0c9996b`
- **工作分支：** `agent/stage-0-ai-workflow`
- **相关方案章节：** 工程落地方案第 6、20、21、23 节

### 目标

建立可由后续 AI 直接读取的执行、验证、交接和防幻觉规则，同时保持上下文精简。本任务不创建 RelayCove 业务代码。

### 已知事实

- `已验证`：仓库当前只有工程文档和仓库治理文件，没有 `.sln` 或可运行代码。
- `已验证`：本机安装 .NET SDK `10.0.101`。
- `已验证`：任务开始时 `main` 指向基准提交，只有此前生成的 `AGENTS.md` 未提交。

### 范围

- 必须实现：
  - 修订简洁的 `AGENTS.md`
  - 创建中文工作流、任务模板、状态页、关键决策索引和独立审查模板
  - 记录本次重要任务的范围和验证证据
- 允许修改：
  - `AGENTS.md`
  - `docs/ai/`
- 明确不做：
  - 不创建解决方案、业务项目、验证脚本或 CI
  - 不推送、不合并、不发布

### 验收标准

- [x] `AGENTS.md` 保持 200–400 词并能指向详细工作流。
- [x] 新 AI 能从状态页识别当前阶段、验证状态、下一任务和权限。
- [x] 模板包含范围、验收、证据、停止条件、结果和后续步骤。
- [x] 工作流明确事实来源、验证声明、分级复核与提交权限。
- [x] 所有 Markdown 相对链接指向现有文件。
- [x] Git 差异不包含业务代码、验证脚本或 CI。

### 停止并询问

- 如果必须修改 `AGENTS.md` 和 `docs/ai/` 以外的文件。
- 如果落地要求与“本地提交但不推送”的权限边界冲突。

## 任务结果

### 修改摘要

- 建立三层上下文：精简根规则、中文 AI 工作流和重要任务日志。
- 增加执行与独立审查提示词、状态交接页和关键架构决策索引。
- 固化事实来源、证据标签、停止条件、分级复核和本地提交权限。

### 验证证据

| 状态 | 命令或场景 | 结果 |
| --- | --- | --- |
| `已验证` | PowerShell 字数与相对链接检查 | `AGENTS.md` 344 词；7 个 Markdown 文件的相对链接均存在 |
| `已验证` | `git diff --check` | 退出码 0，无空白错误 |
| `已验证` | `rg -n '^#{1,3} ' AGENTS.md docs/ai` | 所有指南和模板均具有可导航标题 |
| `已验证` | `git status -sb` 与文件范围检查 | 仅新增 `AGENTS.md` 和 `docs/ai/`；没有业务代码、脚本或 CI |
| `未验证` | `dotnet build` / `dotnet test` | 仓库尚无解决方案或代码，按计划不执行 |

### 文件范围

- 新增：`AGENTS.md`、`docs/ai/` 下的工作流与模板
- 修改：无
- 删除：无

### 决策与限制

- 决策：代码脚手架完成前不创建占位验证脚本或空 CI。
- 已知限制：当前仓库没有可执行代码，因此不能运行构建或测试。

### 下一步

- 使用独立任务创建阶段 0 的 .NET 解决方案脚手架和真实验证入口。
